// criar as variaveis e depois pensar nos cenários

// criar randomNumber, pois o pc percisa escolher um numero primeiro e depois verificar se o numero que eu escolhi, bate com o do pc
let randomNumber = Math.floor(Math.random() * 100)
console.log(randomNumber);

// criar função 
function checkGuess() {
    
}