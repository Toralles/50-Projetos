// item
// lista
// entrada
// color

document.getElementById('').innerHTML
document.createElement('')
item.push(entrada)
item.appdendChild(entrada)
entrada.textContent = 'texto' + i
Math.max(...a)
let randmColor = colors[Math.floor(Math.random() * colors.length)]// randomizar cores pre definidas
